
const ListaReporte = ({ title}) => {
    return (
        <button className="bg-sky-300 p-3 w-full shadow rounded-lg mt-4 flex"
        
        >
            {title}
        </button>
    )
}

export default ListaReporte